require(["dojo/_base/declare",
        "dojo/_base/lang",
        "EAEicnPluginDojo/GlobalInit"],
    function(declare, lang, GlobalInit) {
        /**
         * Use this function to add any global JavaScript methods your plug-in requires.
         */
        /*
            lang.setObject("restartTaskAction", function(repository, items, callback, teamspace, resultSet, parameterMap) {

              * Add custom code for your action here. For example, your action might launch a dialog or call a plug-in service.

            });
        */
    });
